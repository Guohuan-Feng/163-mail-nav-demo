# 163 邮箱净值附件提取（演示版）

这是一个本地、手动执行的最简演示程序。它只做一件事：从 163 邮箱的 INBOX 下载 .xlsx 附件，识别常见净值表头后，生成一张固定格式的 Excel 台账。

它不会发送邮件、不会创建计划任务、不会把邮件标为已读，也不会使用或输出你的邮箱密码。

## 一、输出格式

生成的工作簿只有一个工作表：净值台账。列固定为 10 列：

| 列名 | 含义 |
| --- | --- |
| 基金代码 | 原附件中的基金代码 |
| 基金名称 | 原附件中的基金名称 |
| 产品代码 | 原附件中的产品代码 |
| 产品名称 | 原附件中的产品名称 |
| 净值日期 | 标准日期 |
| 单位净值 | 数值 |
| 累计净值 | 数值，可为空 |
| 来源附件 | 原始附件文件名 |
| 来源工作表 | 原始工作表名称 |
| 来源行号 | 原始 Excel 行号，便于追溯 |

默认只保留前 5 条规范记录，适合现场演示。需要全部记录时，把 config.env 中的 MAX_ROWS 设为 0，或运行时添加 --max-rows 0。

## 二、首次配置

在本目录打开 PowerShell，依次执行：

~~~powershell
py -m pip install -r requirements.txt
Copy-Item config.example.env config.env
notepad config.env
~~~

在 config.env 中只填写：

~~~text
IMAP_USER=你的163邮箱地址
IMAP_AUTH_CODE=你在163邮箱网页端生成的IMAP授权码
~~~

授权码不是网页登录密码。请先在 163 邮箱网页端开启 IMAP 服务，再生成授权码；不要把 config.env 上传、发给他人或提交到代码仓库。

## 三、真实演示：从 163 邮箱提取

~~~powershell
py nav_mail_demo.py
~~~

程序会只读检索最近 7 天的 INBOX、下载 .xlsx 附件到 downloads\时间戳\，并在 output\ 生成：

~~~text
基金净值台账_YYYYMMDD_HHMMSS.xlsx
~~~

可选参数：

~~~powershell
# 只查最近 2 天
py nav_mail_demo.py --days 2

# 输出最多 10 条
py nav_mail_demo.py --max-rows 10

# 输出全部符合条件的记录
py nav_mail_demo.py --max-rows 0
~~~

## 四、无邮箱离线演示

先让程序生成两份脱敏的示例附件（只需一次）：

~~~powershell
py nav_mail_demo.py --make-sample-input .\sample_input
~~~

再执行：

~~~powershell
py nav_mail_demo.py --input-dir .\sample_input
~~~

该流程会生成恰好 5 条、10 列的示例结果；完全不会读取 config.env，也不会连接邮箱，适合先验证格式和流程。

## 五、附件表头要求与限制

演示版会在每个工作表的前 20 行识别表头，至少应包含：

- 净值日期
- 单位净值
- 基金或产品的代码/名称字段之一

常见同义表头（如“估值日期”“每份净值”“累计单位净值”）也会识别。当前最简版本只支持 .xlsx；.xls 会明确提示并跳过。

同一“基金/产品/净值日期”重复出现时，程序保留首次读取的记录并在控制台提示；若同一主键的净值不同，也会明确提示冲突，绝不静默覆盖。

## 六、现场演示建议

1. 先使用“无邮箱离线演示”验证输出格式；
2. 在 163 邮箱里准备一封带 .xlsx 附件的测试邮件；
3. 再运行真实模式；
4. 打开 output 下最新生成的台账，展示固定 10 列与来源追溯字段。

后续优化可以再单独加入：应收邮件清单、未收到/未抓取提醒、历史台账合并、冲突处理、定时任务和通知。它们不在本演示版内。
